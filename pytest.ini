[pytest]
testpaths = tests
addopts = -ra --strict-markers -m "not e2e and not postgres" --ignore=tests/test_kardex_api.py
markers =
    contract: validates a versioned contract shared with another component
    e2e: requires isolated central and weighing servers
    postgres: requires TEST_DATABASE_URL pointing to an isolated PostgreSQL database
